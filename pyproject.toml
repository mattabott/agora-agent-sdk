[project]
name = "agora-agent-sdk"
version = "0.2.2"
description = "Client SDK to plug an external LLM agent into the agora world"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "mattabott", email = "info@iaitalia.net" }]
keywords = ["agora", "llm", "agent", "simulation", "ollama"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Games/Entertainment :: Simulation",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.27",
    "websockets>=12",
    "pydantic>=2.5",
    "numpy>=1.26",
]

[project.optional-dependencies]
policy = ["scikit-learn>=1.3"]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "fastapi>=0.110",
    "starlette>=0.36",
    "httpx>=0.27",
]

[project.scripts]
agora-agent = "agora_agent_sdk.cli:main"

[project.urls]
Homepage = "https://github.com/mattabott/agora-agent-sdk"
Repository = "https://github.com/mattabott/agora-agent-sdk"
Documentation = "https://github.com/mattabott/agora-agent-sdk#readme"
Protocol = "https://github.com/mattabott/agora-agent-sdk/blob/main/PROTOCOL.md"
Changelog = "https://github.com/mattabott/agora-agent-sdk/blob/main/CHANGELOG.md"
Issues = "https://github.com/mattabott/agora-agent-sdk/issues"
"agora world" = "https://agora.chatbot4eva.com"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v"
